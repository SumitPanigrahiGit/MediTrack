{
  "name": "meditrack",
  "version": "1.0.0",
  "description": "MediTrack Healthcare Management System",
  "scripts": {
    "start": "node backend/server.js",
    "build": "cd Frontend && npm install && CI=false npm run build && cd .. && cd backend && npm install && cd ..",
    "postinstall": "cd backend && npm install && cd .. && cd Frontend && npm install && cd .."
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2",
    "express-rate-limit": "^7.1.5",
    "helmet": "^7.1.0",
    "morgan": "^1.10.0",
    "uuid": "^9.0.1"
  }
}
}

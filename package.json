{
  "name": "meditrack",
  "version": "1.0.0",
  "description": "MediTrack Healthcare Management System",
  "scripts": {
    "start": "node backend/server.js",
    "build": "cd Frontend && npm install && CI=false npm run build && cd ..",
    "postinstall": "cd Frontend && npm install && cd .."
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
